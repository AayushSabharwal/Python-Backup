import math
import random
def rand(p):
      flips = 1;
      while(p != int(p)):
            p *= 10
            flips *= 10
      
      if(num10 < p):
            return 'H'
      else:
            return 'T'